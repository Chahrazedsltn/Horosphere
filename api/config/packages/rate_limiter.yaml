framework:
    rate_limiter:
        auth_login:
            policy: 'sliding_window'
            limit: 5
            interval: '1 minute'
        password_reset:
            policy: 'sliding_window'
            limit: 3
            interval: '5 minutes'
